# Modelsim flow

- This **Design Flow** includes Simulation and WaveForm Visualization
  - simulation is using modelsim commands
  - waveform visualization is using modelsim gui

## Setup the Verilog Design Flow

- install Modelsim and add commmands to the path -> to check run `vsim -version`
